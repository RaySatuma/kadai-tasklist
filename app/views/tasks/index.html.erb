<%if logged_in?  %>

<h1>タスク一覧</h1>

<ul>
  <% @task.each do |t| %>
   <li><%= link_to t.id, t %> : <%= t.status %> &gt; <%= t.content %></li>
  <% end %>
    
　<%= link_to "タスクの新規作成" , new_task_path %>
</ul>

<% else %>

  <%= render 'sessions/new' %>
<% end %>