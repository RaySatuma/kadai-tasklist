<h1>第<%= @task.id %>タスクの編集ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, "内容" %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to 'タスク一覧に戻る', tasks_path %>